如何将需要上传的文件上传到gothub中。
打开文件，创建.git文件。
第一步右键git bash here
第二步，git init
第三步可以创建touch .gitignore文件忽略不需要的文件，
第四步git add .
git commit -m "首次提交：STM32外部中断按键控制LED实验"
git remote add origin 仓库地址
git push -u origin main
